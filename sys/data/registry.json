{
  "ui": {
    "launcher": {
        "application": "dolphin",
        "arguments": {
          "launcher": true
        }
    }
  },
  "crash-save": {
    "enable": true,
    "interval": 60000,
    "dump-file": "/users/${USERNAME}/.sysdata/crash-saves/${APPNAME}-${TIMESTAMP}.csd"
  }
}
